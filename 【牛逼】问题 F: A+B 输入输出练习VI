#include<stdio.h>

int main(){
	int N,type,count=0;
	while(scanf("%d",&N)!=EOF){
//		if(N==0)
//		break;
		for(int i=0;i<N;i++){
			scanf("%d",&type);
			count+=type;
		}
		printf("%d\n",count);
		count=0;
	}
	return 0; 
}
